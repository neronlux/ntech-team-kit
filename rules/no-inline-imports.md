---
description: Keep imports at top of file and avoid inline imports
---

# No inline imports

Always place imports at the top of the module. Avoid inline imports in function bodies, type annotations, or interface fields unless a strict circular dependency requires them. Add a comment for that exception.
